apiVersion: v1
kind: Service
metadata:
  name: two-tier-app-service

spec:
  type: NodePort
  selector:
    app: two-tier-app
  ports:
    - port: 5000        # cluster port
      targetPort: 5000  # container port
      nodePort: 30007   # port exposed on worker node

